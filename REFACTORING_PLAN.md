# 🏗️ 기술진단팀 협업시스템 리팩토링 계획

## 🎯 목표
- 버전업 시 안전한 구조 확보
- 기능별 모듈화로 영향 범위 최소화
- 에이전트 개발 효율성 극대화

## 📋 현재 문제점
- 컴포넌트 무분별한 배치 (15개 파일 루트에 존재)
- 기능별 분리 없음
- 중복 컴포넌트 존재
- 버전업 시 영향 범위 불명확

## 🏗️ 새로운 구조

### 🚫 Core (절대 건드리지 말아야 할 부분)
```
src/core/
├── app/                   # Next.js App Router
│   ├── layout.tsx        # 기본 레이아웃
│   ├── page.tsx          # 메인 페이지
│   ├── globals.css       # 전역 스타일
│   └── providers/        # 세션 프로바이더
├── lib/                   # 핵심 라이브러리
│   ├── auth.ts           # 인증 시스템
│   ├── prisma.ts         # 데이터베이스
│   ├── api-client.ts     # API 클라이언트
│   └── google-api.ts     # Google Drive 연동
└── types/                 # 기본 타입
    ├── database.ts       # DB 스키마
    ├── api.ts            # API 타입
    └── common.ts         # 공통 타입
```

### ✅ Features (안전하게 개발할 기능 모듈)
```
src/features/
├── auth/                  # 인증 기능
│   ├── components/
│   │   ├── LoginForm.tsx
│   │   └── RegisterForm.tsx
│   ├── hooks/
│   │   └── useAuth.ts
│   └── services/
│       └── auth.service.ts
├── projects/              # 프로젝트 관리
│   ├── components/
│   │   ├── ProjectManager.tsx
│   │   ├── ProjectListView.tsx
│   │   └── CompactProjectManager.tsx
│   ├── hooks/
│   │   └── useProjects.ts
│   └── services/
│       └── project.service.ts
├── users/                 # 사용자 관리
│   ├── components/
│   │   ├── UserManagement.tsx
│   │   └── UserProfile.tsx
│   └── services/
│       └── user.service.ts
├── files/                 # 파일 관리
│   ├── components/
│   │   └── FileManager.tsx
│   └── services/
│       └── file.service.ts
├── calendar/              # 캘린더/일정
│   ├── components/
│   │   ├── CalendarManager.tsx
│   │   └── ScheduleTaskManager.tsx
│   └── services/
│       └── calendar.service.ts
├── board/                 # 게시판
│   ├── components/
│   │   ├── FreeBoard.tsx
│   │   └── GlobalBoard.tsx
│   └── services/
│       └── board.service.ts
└── chat/                  # 채팅
    ├── components/
    │   └── ChatInterface.tsx
    └── services/
        └── chat.service.ts
```

### ✅ Shared (공용 컴포넌트)
```
src/shared/
├── components/            # UI 컴포넌트
│   ├── ui/
│   │   ├── Modal.tsx
│   │   ├── Button.tsx
│   │   └── Input.tsx
│   └── common/
│       └── PermissionComponents.tsx
├── hooks/                 # 커스텀 훅
│   └── common.ts
├── utils/                 # 유틸리티
│   ├── validators.ts
│   └── constants.ts
└── styles/               # 공용 스타일
    └── globals.css
```

### ✅ Layout (레이아웃)
```
src/layout/
├── components/
│   ├── TitleBar.tsx
│   ├── Header.tsx
│   ├── LeftSidebar.tsx
│   ├── MainContent.tsx
│   ├── RightSidebar.tsx
│   ├── StatusBar.tsx
│   └── TabNavigation.tsx
└── MainLayout.tsx
```

## 🔄 이전 계획

### 1단계: 구조 준비
- 새로운 폴더 구조 생성
- 기존 파일 분석 및 이동 계획 수립

### 2단계: Core 보호
- core 폴더에 핵심 파일 복사
- 기존 구조와의 호환성 유지

### 3단계: Features 이전
- 기능별로 컴포넌트 이전
- 의존성 확인 및 수정

### 4단계: Shared 정리
- 공용 컴포넌트 통합
- 중복 코드 제거

### 5단계: Layout 정리
- 레이아웃 컴포넌트 통합
- UI/UX 적용 준비

## 🎯 버전업 시 영향 범위

### 🚫 Core (변경 주의)
- 인증 시스템 수정 시 전체 영향
- 데이터베이스 스키마 변경 시 마이그레이션 필요
- API 변경 시 모든 클라이언트 영향

### ✅ Features (안전한 개발)
- 각 기능 모듈은 독립적 개발 가능
- 다른 기능에 영향 없음
- 테스트 및 배포 용이

### ✅ Shared (안전한 재사용)
- 공용 컴포넌트 수정 시 사용처만 영향
- 버전 관리 용이
- 재사용성 극대화

### ✅ Layout (UI/UX 전용)
- UI/UX 변경 시에만 수정
- 기능 로직과 분리
- 디자인 시스템 적용 용이

## 💡 에이전트 개발 가이드

### ✅ 권장 작업
- features/ 내에서 새 기능 개발
- shared/ 컴포넌트 재사용
- layout/에서 UI/UX 수정

### 🚫 금지 작업
- core/ 직접 수정 (필요시 겸손하게 요청)
- features 간 직접 의존성 생성
- 전역 상태 무분별한 수정

## 🎯 성공 기준
- 각 기능 모듈 독립적 동작
- 버전업 시 영향 범위 명확
- 에이전트 개발 효율성 50% 향상
